#	TextEditor - A syntax highlighting text editor for Dear ImGui.
#	Copyright (c) 2024-2026 Johan A. Goossens. All rights reserved.
#
#	This work is licensed under the terms of the MIT license.
#	For a copy, see <https://opensource.org/licenses/MIT>.


import datetime, fileinput, glob, os, re, sys


rangePattern = re.compile(r"([0-9A-F]+)\.\.([0-9A-F]+)")


codePointHeader = '''//	Do not edit this file, it is automatically generated

//	TextEditor - A syntax highlighting text editor for Dear ImGui.
//	Copyright (c) 2024-2026 Johan A. Goossens. All rights reserved.
//
//	This work is licensed under the terms of the MIT license.
//	For a copy, see <https://opensource.org/licenses/MIT>.


#pragma once


//
//	Tables below are automatically generated from the unicode database version %s
//	downloaded from http://www.unicode.org/Public/UNIDATA on %s
//	using the GenerateTables.py script.
//


//
//	Include files
//

#include <iterator>

#include "imgui.h"


//
//	Range table types
//

template <typename T>
struct Range {
	T low;
	T high;
};

using Range16 = Range<ImWchar16>;
using Range32 = Range<ImWchar32>;

template <typename T>
struct StrideRange {
	T low;
	T high;
	T stride;
};

using StrideRange16 = StrideRange<ImWchar16>;
using StrideRange32 = StrideRange<ImWchar32>;

template <typename T>
struct CaseRange {
	T low;
	T high;
	int32_t toUpper;
	int32_t toLower;
};

using CaseRange16 = CaseRange<char16_t>;
using CaseRange32 = CaseRange<char32_t>;
'''

lineBreakHeader = '''//	Do not edit this file, it is automatically generated

//	TextEditor - A syntax highlighting text editor for Dear ImGui.
//	Copyright (c) 2024-2026 Johan A. Goossens. All rights reserved.
//
//	This work is licensed under the terms of the MIT license.
//	For a copy, see <https://opensource.org/licenses/MIT>.


#pragma once


//
//	Tables below are automatically generated from the unicode database version %s
//	downloaded from http://www.unicode.org/Public/UNIDATA/ on %s
//	using the GenerateTables.py script.
//


//
//	Include files
//

#include <iterator>

#include "imgui.h"

#include "LBC.h"


//
//	Table types
//

template <typename T>
struct LineBreakRange {
	T low;
	T high;
	LBC lbc;
};

using LineBreakRange16 = LineBreakRange<ImWchar16>;
using LineBreakRange32 = LineBreakRange<ImWchar32>;
'''


def fetch(f):
	if not os.path.exists(os.path.basename(f)):
		os.system("curl -O http://www.unicode.org/Public/UNIDATA/%s" % f)

	if not os.path.exists(os.path.basename(f)):
		sys.stderr.write("cannot load %s" % f)
		exit(1)


def purge(dir, pattern):
	for f in glob.glob(pattern):
		os.remove(os.path.join(dir, f))


def buildRangeTable(input, filter):
	codepoints = []
	fetch(input)

	for text in fileinput.input(os.path.basename(input)):
		line = [entry.strip() for entry in text.split("#")[0].split(';')]

		if len(line) > 1 and filter(line):
			m = rangePattern.match(line[0])

			if m:
				for n in range(int(m.group(1), 16), int(m.group(2), 16) + 1):
					codepoints.append(n)

			else:
				codepoints.append(int(line[0], 16))

	codepoints = sorted(set(codepoints))
	ranges = []

	for codepoint in codepoints:
		ranges.append([codepoint, codepoint, 1])

	return ranges


def calculateStrides(table):
	result = []
	current = table.pop(0)

	while len(table):
		next = table.pop(0)
		stride = next[0] - current[1]

		if (next[0] == next[1] or stride == next[2]) and (current[0] == current[1] or stride == current[2]):
			current[1] = next[1]
			current[2] = stride

		elif stride == current[2]:
			current[1] = next[0]
			current[2] = stride
			next[0] = next[0] + next[2]

			if next[0] <= next[1]:
				result.append(current)
				current = next

		else:
			result.append(current)
			current = next

	result.append(current)
	return result


def buildRanges(input, filter):
	fetch(input)

	ranges = []
	lastFirst = None
	lastSecond = None
	lastValue = None

	for text in fileinput.input(os.path.basename(input)):
		line = [entry.strip() for entry in text.split("#")[0].split(';')]

		if len(line) > 1 and filter(line):
			m = rangePattern.match(line[0])
			value =  line[1].lower()

			if m:
				first = int(m.group(1), 16)
				second = int(m.group(2), 16)

			else:
				first = second = int(line[0], 16)

			if lastValue is None:
				lastFirst = first
				lastSecond = second
				lastValue = value

			elif lastSecond + 1 != first or lastValue != value:
				ranges.append((lastFirst, lastSecond, lastValue))
				lastFirst = first
				lastSecond = second
				lastValue = value

			else:
				lastSecond = second

	ranges.append((lastFirst, lastSecond, lastValue))
	return ranges


def buildCaseRanges(input):
	result = []
	codepoints = []
	fetch(input)

	for text in fileinput.input(os.path.basename(input)):
		line = [entry.strip() for entry in text.split("#")[0].split(';')]

		if len(line) > 1:
			codepoint = int(line[0], 16)

			if line[2] == "Lu":
				if len(line[13]):
					result.append([codepoint, codepoint, 0, int(line[13], 16) - codepoint])

			elif line[2] == "Ll":
				if len(line[12]):
					result.append([codepoint, codepoint, int(line[12], 16) - codepoint, 0])

	return result


def compressRanges(ranges):
	result = []
	current = ranges.pop(0)

	while len(ranges):
		next = ranges.pop(0)

		if next[0] == current[1] + 1 and next[2] == current[2] and next[3] == current[3]:
			current[1] = next[0]

		else:
			result.append(current)
			current = next

	result.append(current)
	return result


def compressUpperLower(ranges):
	result = []
	inAdjacent = False
	current = ranges.pop(0)
	start = end = 0

	while len(ranges):
		next = ranges.pop(0)

		if ((current[2] ==  0 and current[3] == 1 and next[2] == -1 and next[3] == 0) or
			(current[2] == -1 and current[3] == 0 and next[2] ==  0 and next[3] == 1)):

			if not inAdjacent:
				start = current[0]
				inAdjacent = True

			end = next[1]

		elif inAdjacent:
			result.append([start, end, 0xffff, 0xffff])
			inAdjacent = False

		else:
			result.append(current)

		current = next

	if inAdjacent:
		result.append([start, end, 0xffff, 0xffff])

	else:
		result.append(current)

	return result


def printTable(table, type, name, filter, format, conditional32bit):
	result = '''

//
//	%s
//

''' % name

	if conditional32bit:
		result += "#if defined(IMGUI_USE_WCHAR32)\n\n"

	result += "static %s %s[] = {\n" %(type, name)
	line = "\t"
	first = True

	for entry in table:
		if filter(entry):
			chunk = format(entry)

			if len(line) + len(chunk) < 120:
				if first:
					line += chunk
					first = False

				else:
					line += ", " + chunk

			else:
				result += line + ",\n"
				line = "\t" + chunk

	result += line + "\n};\n"

	if conditional32bit:
		result += "\n#endif\n"

	return result


def printLookup(table, type, name, default, filter, format):
	lookup = [default] * 256

	for entry in table:
		if filter(entry):
			for glyph in range(entry[0], min(entry[1] + 1, 256)):
				lookup[glyph] = format(entry[2])

	result = '''

//
//	%s
//

''' % name

	result += "static %s %s[] = {\n" %(type, name)

	line = "\t"
	first = True

	for chunk in lookup:
		if len(line) + len(chunk) < 120:
			if first:
				line += chunk
				first = False

			else:
				line += ", " + chunk

		else:
			result += line + ",\n"
			line = "\t" + chunk

	result += line + "\n};\n"
	return result


def printSet(table, format):
	result = ""
	line = "\t"
	first = True

	for entry in table:
		for glyph in range(entry[0], entry[1] + 1):
			chunk = format(glyph)

			if len(line) + len(chunk) < 120:
				if first:
					line += chunk
					first = False

				else:
					line += ", " + chunk

			else:
				result += line + ",\n"
				line = "\t" + chunk

	result += line + "\n"
	return result


if __name__ == "__main__":
	os.chdir(os.path.dirname(os.path.abspath(__file__)))

	fetch("ReadMe.txt")

	with open("ReadMe.txt") as readme:
		version = re.search(r"for Version (\d+\.\d+\.\d+) of the Unicode", readme.read()).group(1)

	filter8 = lambda r : r[0] < 0x100
	filter16 = lambda r : r[0] < 0x10000
	filter32 = lambda r : r[0] >= 0x10000
	formatStride = lambda r : ("{0x%04x, 0x%04x, 0x%04x}" if r[0] < 0x10000 else "{0x%05x, 0x%05x, 0x%04x}") % (r[0], r[1], r[2])
	formatLineBreak = lambda r : ("{0x%04x, 0x%04x, LBC::%s}" if r[0] < 0x10000 else "{0x%05x, 0x%05x, LBC::%s}") % (r[0], r[1], r[2])
	formatCase = lambda r : ("{0x%04x, 0x%04x, %6d, %6d}" if r[0] < 0x10000 else "{0x%05x, 0x%05x, %3d, %2d}") % (r[0], r[1], r[2], r[3])
	formatRange = lambda r : ("{0x%04x, 0x%04x}" if r[0] < 0x10000 else "{0x%05x, 0x%05x}") % (r[0], r[1])
	formatClass = lambda r : "LBC::%s" % (r)
	formatGlyph = lambda r : ("0x%04x" if r < 0x10000 else "0x%05x") % (r)

	with open("CodePointTables.h", "w") as tables:
		tables.write(codePointHeader % (version, datetime.datetime.now().strftime("%A, %B %d, %Y")))

		letters = calculateStrides(buildRangeTable("UnicodeData.txt", lambda line : line[2][0] == "L"))
		tables.write(printTable(letters, "StrideRange16", "letters16", filter16, formatStride, False))
		tables.write(printTable(letters , "StrideRange32", "letters32", filter32, formatStride, True))

		lower = calculateStrides(buildRangeTable("UnicodeData.txt", lambda line : line[2] == "Ll"))
		tables.write(printTable(lower, "StrideRange16", "lower16", filter16, formatStride, False))
		tables.write(printTable(lower , "StrideRange32", "lower32", filter32, formatStride, True))

		upper = calculateStrides(buildRangeTable("UnicodeData.txt", lambda line : line[2] == "Lu"))
		tables.write(printTable(upper, "StrideRange16", "upper16", filter16, formatStride, False))
		tables.write(printTable(upper, "StrideRange32", "upper32", filter32, formatStride, True))

		numbers = calculateStrides(buildRangeTable("UnicodeData.txt", lambda line : line[2] == "Nd"))
		tables.write(printTable(numbers, "StrideRange16", "numbers16", filter16, formatStride, False))
		tables.write(printTable(numbers, "StrideRange32", "numbers32", filter32, formatStride, True))

		whitespace = calculateStrides( buildRangeTable("PropList.txt", lambda line : line[1] == "White_Space"))
		tables.write(printTable(whitespace, "StrideRange16", "whitespace16", filter16, formatStride, False))

		xidStart = calculateStrides(buildRangeTable("DerivedCoreProperties.txt", lambda line : line[1] == "XID_Start"))
		tables.write(printTable(xidStart, "StrideRange16", "xidStart16", filter16, formatStride, False))
		tables.write(printTable(xidStart, "StrideRange32", "xidStart32", filter32, formatStride, True))

		xidContinue = calculateStrides(buildRangeTable("DerivedCoreProperties.txt", lambda line : line[1] == "XID_Continue"))
		tables.write(printTable(xidContinue, "StrideRange16", "xidContinue16", filter16, formatStride, False))
		tables.write(printTable(xidContinue, "StrideRange32", "xidContinue32", filter32, formatStride, True))

		eastAsian = compressRanges(buildRanges("EastAsianWidth.txt", lambda line : line[1] in ["F", "W", "H"]))
		tables.write(printTable(eastAsian, "Range16", "eastAsian16", filter16, formatRange, False))
		tables.write(printTable(eastAsian, "Range32", "eastAsian32", filter32, formatRange, True))

		caseRanges = compressRanges(buildCaseRanges("UnicodeData.txt"))
		tables.write(printTable(caseRanges, "CaseRange16", "case16", filter16, formatCase, False))
		tables.write(printTable(caseRanges, "CaseRange32", "case32", filter32, formatCase, True))

	with open("LineBreakTables.h", "w") as tables:
		tables.write(lineBreakHeader % (version, datetime.datetime.now().strftime("%A, %B %d, %Y")))

		# pf = buildRangeTable("UnicodeData.txt", lambda line : line[2] == "Pf")
		# print(printSet(pf, formatGlyph))

		# pi = buildRangeTable("UnicodeData.txt", lambda line : line[2] == "Pi")
		# print(printSet(pi, formatGlyph))

		lineBreak = compressRanges(buildRanges("LineBreak.txt", lambda line: True))
		tables.write(printLookup(lineBreak, "LBC", "lineBreak8", "LBC::xx", filter8, formatClass))
		tables.write(printTable(lineBreak, "LineBreakRange16", "lineBreak16", filter16, formatLineBreak, False))
		tables.write(printTable(lineBreak, "LineBreakRange32", "lineBreak32", filter32, formatLineBreak, True))

	purge(".", "*.txt")
