Circuits are grouped in categories that make it easier to find them
and recognize them in a graph. Each category has a unique color as
shown on the right below.

![screenshot](img/circuit-categories.png#right)

- **Input Nodes** - These circuits enable users to input values or
select assets (e.g. sound files). If a circuit of circuits is used
in another object (e.g. a 3D scene), input fields enable
users to interact with the circuit's functionality without having the
understand the full logic. This in essence makes a circuit
a macro or sub-function somewhere else.
- **Output Nodes** - These circuits enable the user to save signals
or route them to computer peripherals.
- **Generator Nodes** - These circuits generate signals based
on provided parameters.
- **Processor Nodes** - These circuits process one or more signals
and generate a new signal (e.g. equalizer, filter, mixer).
- **Effect Nodes** - These circuits apply an effect to a signal
(e.g. echo, distortion).
- **Converter Nodes** - These circuits converter the signal flow
from one type to another (e.g. mono to stereo).
- **Probe Nodes** - These circuits provide debugging support by
visualizing various signal types.
